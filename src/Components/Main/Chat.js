import React from "react";
import styled from "styled-components";
const ChatContainer = styled.div``;

const Chat = () => {
  return (
    <div>
      <ChatContainer>Welcome</ChatContainer>
    </div>
  );
};

export default Chat;
